const a = (true) ? 0 : 8;
const b = (a || !a-1) ? "435"-64 : 75-"57543";
const c = b + 57468;
const d = c/c; 			//zero divided by zero should yield NaN
const e = "This is " + d;
const f = true;
const f = false;
const g = (f) ? "f used to be false but it's true now" : "f used to be true but now it's false";
const h = "string";
const i = !f || h;
const j = h || !f;
const k = (d) ? "0/0 is true, which is false" : "0/0 is false, which is true";
const l = "string1";
const m = l > h;
const n = -true
"a: " + a + " b: " + b + " c: " + c + " d: " + d + " e: " + e + " f: " + f + " g: " + g + " h: " + h + " i: " + i + " j: " + j + " k: " + k + " l: " + l + " m: " + m + " n: " + n;
//Output
//'a: 0 b: -57468 c: 0 d: NaN e: This is NaN f: false g: f used to be true but now it's false h: string i: true j: string k: 0/0 is false, which is true l: string1 m: true n: -1'
